#if ($rally || $raid)#if ($raid)A etapa #else O tramo cronometrado #end<b>$session.shortname - $session.name ($session.duration km)</b>#end#if (!$rally && !$raid)A sesión <b>$session.name</b>#end pertencente a <b>$session.eventEdition.longEventName</b> comeza en #if ($minutesToStart == 15)15 minutos#end#if ($minutesToStart == 60)1 hora #end#if ($minutesToStart == 180)3 horas#end#if (!$rally && !$raid) en <b>$session.eventEdition.trackLayout.racetrack.name</b>#end#if ($logoUrl != $null)

<a href="$logoUrl">&#8205;</a>#end
