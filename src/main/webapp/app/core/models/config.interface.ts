export interface InConfiguration {
  layout: {
    rtl: boolean;
    variant: string;
    theme_color: string;
    logo_bg_color: string;
    sidebar: {
      collapsed: boolean;
      backgroundColor: string;
    };
  };
}
